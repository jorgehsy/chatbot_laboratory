python >= 3.8
sqlalchemy >= 2.0.0
anthropic >= 0.18.0  # For Claude API
openai >= 1.12.0    # For GPT API
psycopg2-binary    # For PostgreSQL connection
pydantic >= 2.0.0  # For data validation
python-dotenv      # For environment variables
asyncio            # For async operations